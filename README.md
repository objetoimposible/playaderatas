# Pandoc Web CMS

## COmpilación

via comando gulp en terminal se realiza la compialción del proyecto pandoc en /dist, se vigila via BrowserSync cambios en la raíz, y se sirven los archivos al repositorio github.